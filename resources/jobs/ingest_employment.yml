resources:
  jobs:
    ingest_employment:
      name: "store-siting-ingest-employment"
      schedule:
        quartz_cron_expression: "0 0 6 1 1,4,7,10 ?"
        timezone_id: "America/New_York"
      tasks:
        - task_key: ingest
          spark_python_task:
            python_file: src/jobs/ingest_employment.py
            source: WORKSPACE
          existing_cluster_id: ${var.cluster_id}
